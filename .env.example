# FoodLink API Environment Variables
# Copy this file to .env and update with your values

# Server Configuration
PORT=3000

# JWT Secret (IMPORTANT: Change this in production!)
# Generate a secure random string: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-secret-key-change-in-production

# Database
# SQLite database file will be created automatically at ./foodlink.db
# No additional configuration needed for SQLite
