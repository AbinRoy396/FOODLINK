// Test file to verify imports work
import 'package:flutter/material.dart';
import 'package:food_link_app/main.dart';

void main() {
  // Simple test to check if the app compiles
  runApp(const MaterialApp(home: Scaffold(body: Center(child: Text('Test')))));
}
